import styled from "styled-components";

const Input = styled.input`
    border: none;
    font-family: 'Pretandard Variable';
    position: relative;
    bottom: 4px;
    font-weight: 800;

    :focus{
        outline: none;
    }

    
`;

export {Input}